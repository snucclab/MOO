problem: >
  서로 다른 두 수 <unknown.0>, <unknown.1>(이)가 주어졌다. 네 자리 수로만 만든 뺄셈식 <equation.0>에서 <unknown.0>(와)과 <unknown.1>의 합을 구하여라.

variable-sampling: null

list-sampling: null

function-call: unk_digit_equation_multi(2,4,'-')

equations:
  "R0: LIST()
  R1: APPEND(R0, <equation.0>
  R2: CALL_SYMPY(R1, <unknown.0>)
  R3: CALL_SYMPY(R1, <unknown.1>)
  R4: ADD(R2, R3)
  R5: PRINT(R4)"