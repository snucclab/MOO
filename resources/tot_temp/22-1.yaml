problem: >
  두 서로 다른 임의의 수 <unknown.0>, <unknown.1>(이)가 있습니다. 두 자리 수 만으로 구성한 뺄셈식 <equation.0>이 다음과 같이 주어졌다고 할 때, <unknown.0>(와)과 <unknown.1>의 합을 구하시오.

variable-sampling: null

list-sampling: null

function-call: unk_digit_equation_multi_bk(2,2,2,'-', 'equation', 'unknown')

equations:
  "R0: LIST()
  R1: APPEND(R0, <equation.0>)
  R2: CALL_SYMPY(R1, <unknown.0>)
  R3: CALL_SYMPY(R1, <unknown.1>)
  R4: ADD(R2, R3)
  R5: PRINT(R4)"
