problem: <num.0>, <num.1> 두 수로 두 자리 수를 나누었습니다. 이 때 나누어 떨어지는 수의 개수는 얼마입니까?

variable-sampling:
  num.0:
    range: [2, 5]
    under-decimal: 0
    type: int
  num.1:
    range: [6, 9]
    under-decimal: 0
    type: int

list-sampling: null

function-call: null

equations:
  "R0: POW(10,2)
  R1: RANGE(10,R0,1)
  R2: LIST()
  R3: APPEND(R2,<num.0>)
  R4: APPEND(R3,<num.1>)
  R5: COUNT_MULTI(R1,R4)
  R6: PRINT(R5)"