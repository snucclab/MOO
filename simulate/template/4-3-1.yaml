problem: <num.0>(와)과 <num.1>(으)로 나눈 나머지가 0인 두 자리 수는 총 몇 개입니까?

variable-sampling:
  num.0:
    range: [2, 5]
    under-decimal: 0
    type: int
  num.1:
    range: [6, 9]
    under-decimal: 0
    type: int

list-sampling: None

function-call: None

equations:
  "R0: RANGE(10,100,1)
  R1: LIST()
  R2: APPEND(R1,<num.0>)
  R3: APPEND(R2,<num.1>)
  R4: COUNT_MULTI(R0,R3)
  R5: PRINT(R4)"